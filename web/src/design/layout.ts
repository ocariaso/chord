/**
 * The design's one breakpoint (design.md#responsive). Below it the results lock to the Mixer and
 * the stylesheet stacks the stem rows, and the landing and processing screens take their phone
 * arrangement. It must equal the @media width in chord-theme.css and the `max-[720px]:` utilities in App,
 * MixerView and AnalysisBar.
 */
export const PHONE_QUERY = "(max-width: 720px)";
